# Figma 작업 규칙 — BLUESOFT DS Master

**Figma 를 고치는 사람이 읽는 문서다.** SCSS 는 `SCSS.md` 에 있다.

**이 문서에는 값·수치가 없다.** 값은 Figma 를 조회해서 확인한다.

Figma API 가 실제로 어떻게 동작하는지(조회·쓰기·검증·삭제 절차, 플러그인 제약,
대비 기준, 아이콘 규격, 레이어 네이밍)는 **`figma-work-principles` 스킬**에 있다.
프로젝트와 무관한 공통 지식이라 저장소에 두지 않는다. 이 문서에는 DS Master 고유 설계만 있다.

---

## 토큰 계층

```
Primitive          색 램프·space·radius·font·motion. hex 가 있는 유일한 곳
  ↓ 별칭
Theme (시맨틱)     surface · text · border · icon · brand · accent
  ↓ 별칭
Theme (컴포넌트)   button · card · field · nav · table · tabs …
  ↓ 참조
컴포넌트 / CSS
```

### 컴포넌트는 시맨틱을 거친다

프리미티브를 직접 참조하면 **다크모드에서 그대로 남는다.**
같은 값의 시맨틱 토큰이 있으면 그것을 경유한다.

```scss
.btn-primary {
    background: var(--blue-600);
} // 금지
.btn-primary {
    background: var(--button-primary-bg);
} // 정상
```

### 프리미티브 직접 참조가 정상인 경우

| 유형                                         | 이유                                      |
| -------------------------------------------- | ----------------------------------------- |
| 시맨틱 계층 자체                             | 프리미티브를 가리키는 것이 이 계층의 역할 |
| 브랜드 램프 (`brand/*` · `accent/*`)         | 브랜드 색을 바꾸는 지점                   |
| 의미색 (성공·경고·오류·주말)                 | 의미가 색에 있음                          |
| 브랜드 자산 (로고·소셜)                      | 색이 고정. 모드 무관                      |
| 유채색 배경 위 글자                          | 대비 확보용 고정                          |
| 컴포넌트 고유 톤                             | 대응하는 시맨틱이 없음                    |
| 알파 토큰                                    | Figma 별칭으로 알파를 못 넣음             |
| 색이 아닌 것 (`font/*`·`space/*`·`radius/*`) | 모드가 없음                               |

**색이 아닌 프리미티브는 검사 대상이 아니다.**

### 형제 참조를 만들지 않는다

같은 그룹의 다른 하위그룹을 참조하면 두 Variant 가 묶여 하나를 바꿀 때 다른 것도 바뀐다.

```
tabs/pill/fg → tabs/underline/fg   // 금지 — pill 과 underline 이 결합
tabs/pill/fg → text/tertiary       // 정상 — 공통 조상을 각자 참조
```

**값이 같아도 Variant 별로 독립 토큰을 둔다.** 토큰 개수는 늘지만 별칭이라 유지 비용이 낮다.

### 독립과 시맨틱 경유는 충돌하지 않는다

Variant 별 독립 토큰이 **같은 시맨틱을 각자 참조**하면 둘 다 만족한다.

```
tabs/underline/fg  ┐
tabs/pill/fg       ├→ text/tertiary
tabs/segmented/fg  ┘
```

`text/tertiary` 를 바꾸면 셋 다 바뀌지만, `pill` 만 조정하고 싶으면
`tabs/pill/fg` 의 참조 대상만 바꾸면 된다. **형제를 거치면 이게 불가능하다.**

---

## 컬렉션과 모드

| 컬렉션     | CSS                             |
| ---------- | ------------------------------- |
| Primitive  | `:root`                         |
| Theme      | `:root` + `[data-theme="dark"]` |
| Shape      | `:root` + `[data-shape="…"]`    |
| Breakpoint | `:root` + 미디어쿼리            |

**모드는 실제로 값이 갈릴 때만 만든다.** 프로젝트마다 자동으로 추가하지 않는다.

플랜에 따라 컬렉션당 모드 개수 상한이 있다 — 필요하면 Figma 요금제 문서를 확인한다.
**모드를 줄이는 이유는 상한이 아니라 유지 비용이다** — 모드가 하나 늘면 앞으로 추가하는
모든 토큰에 값을 하나씩 더 채워야 하고, 그 값이 앞 모드와 같으면 아무 일도 하지 않는 칸이 는다.

### 모드와 프레임 폭은 다르다

**모드는 값이 갈리는 지점이고, 프레임 폭은 시안을 그리는 캔버스 크기다.**
1920 으로 시안을 그린다고 해서 1920 모드가 필요한 것은 아니다.
프레임 폭을 1920 으로 두고 모드는 그보다 낮은 것을 쓰면 된다 — ADR-026.

**`breakpoint` 토큰은 시안과 코드에서 쓰임이 다르다.**

| 쓰임 | 방식 |
|---|---|
| 시안 | 아트보드 프레임의 `width` 에 바인딩한다. 프레임에 모드를 지정하면 폭이 따라온다 |
| 코드 | 미디어쿼리 분기값으로 참조하되 **최상단(Wide)은 쓰지 않는다** |

**최상단 값을 미디어쿼리로 쓰면 브라우저마다 결과가 갈린다.**
W3C 스펙은 미디어쿼리 폭에 세로 스크롤바를 포함하지만 **Chrome · Safari 데스크톱은 제외한다.**
1920 모니터 전체화면에서 `min-width: 1920px` 은 Chrome 에서 안 걸리고 Firefox 에서 걸린다. <!-- 예외: 브라우저 동작 실측이라 폭 값이 서술의 내용 자체다 -->
업계 최상단 분기값이 Tailwind 1536 · Bootstrap 1400 · Material 1600 인 이유가 이것이다.
Wide 위로는 다른 값이 Desktop 과 같으므로 **분기 자체를 만들지 않는다.**

**부모 폭을 따라가는 노드에는 폭 변수를 물리지 않는다.**
`layoutSizingHorizontal = FILL` 인 노드가 `width` 를 `breakpoint` 에 바인딩하고 있으면 둘이 모순된다.
아트보드에 얹히는 컴포넌트는 아트보드 폭을 따라가면 되고, 자기가 브레이크포인트를 알 필요가 없다.
**FILL 로 바꿔도 폭 바인딩은 자동으로 풀리지 않는다** — `setBoundVariable('width', null)` 로 따로 푼다.

**풀블리드는 모드로 만들지 않는다.** 폭이 커질수록 여백이 연속적으로 커지는 동작이라
계단을 만들 이유가 없다. `padding-inline: clamp(…)` 또는
`grid-template-columns: 1fr min(콘텐츠폭, 100%) 1fr` 로 처리한다.

### `Theme=dark` 는 다크모드가 아니다

`Header` · `_Nav/Menu Item` · `_Side Nav/Item` · `Tooltip` 의 `Theme` 축은
**"어두운 스타일"** 이다. 라이트모드 사이트에서도 어두운 헤더로 쓴다.

```
Theme=light  →  모드를 따라감 (다크모드에서 어두워짐)
Theme=dark   →  항상 어두움
```

**따라서 `*/dark/*` 토큰은 다크모드에서 반전하지 않는다.** 반전하면 "어두운 스타일"이 흰색이 된다.
`tooltip/light/*` 도 마찬가지다 — "밝은 스타일"이므로 어두워지면 안 된다.

**검사 규칙:** 토큰명에 `/dark/` 또는 `/light/` 가 들어가면 라이트·다크 값이 같아야 한다.

**주의:** `brand/*` · `accent/*` 램프는 다크모드에서 반전된다.
"어두운 스타일" 토큰이 이것을 참조하면 **별칭은 같아도 값이 뒤집힌다.**
프리미티브(`blue/*` 등)로 고정한다.

코드에서는 `.header--dark` 와 `[data-theme="dark"]` 를 구분해서 쓴다.

`Logo` 의 `Color` 축은 성격이 다르다 — 로고 색상 선택이다.

### light 와 dark 의 hover 동작이 다르다

nav 계열에서 **light 만 hover 시 글자색이 바뀐다.** dark 는 전 상태 고정이다.
이 비대칭을 코드에 반영한다.

---

## 대비 기준

기준표는 `figma-work-principles` 스킬 「H. 대비」.

**아이콘 전용 토큰이 따로 있는 이유가 이 기준 차이다.** `text/*` 를 아이콘에 재사용하면
기준이 섞여 나중에 아이콘만 조정할 수 없다.

`fg` / `bg` 쌍은 **라이트·다크 양쪽에서** 검사한다. 한쪽만 보면 놓친다.

알파 배경 처리도 같은 절에 있다 — **알파 배경은 계산하지 않는다.**

---

## 슬롯과 크기

**슬롯은 이름이 아니라 사이징으로 고른다.**

`Table/Cell` 의 슬롯 이름은 실제 용도와 어긋난다.
인스턴스 오버라이드 유실 위험 때문에 이름을 유지하기로 확정했다.

| 사이징 | 넣을 수 있는 것                      |
| ------ | ------------------------------------ |
| FIXED  | 크기가 고정된 것 (아이콘·아바타 1개) |
| HUG    | 폭이 가변인 것 (Chip · Avatar Group) |

**FIXED 슬롯에 가변폭 컴포넌트를 넣으면 눌린다.**

SLOT 안의 인스턴스는 폭 조절이 제한된다.
컬럼 폭이 다른 테이블은 `Table/Row` 를 쓰지 않고 **셀 인스턴스를 오토레이아웃으로 나열한다.**
디태치가 아니다 — 셀은 인스턴스로 남아 마스터 변경을 계속 받는다.

### 높이는 고정하지 않는다

**`min-height` 로 둔다.** 고정하면 텍스트가 여러 줄일 때 잘린다.
한 줄일 때는 고정과 같고 여러 줄일 때만 늘어난다.

**여백은 `padding-y` 가 아니라 세로 중앙 정렬이 만든다.**
둘을 함께 쓰면 여러 줄에서 높이가 과도해진다.

### Figma 시안값과 CSS 구조값을 구분한다

Figma 는 `width: 100%` 를 표현할 수 없어 브레이크포인트별 실제 폭이 필요하다.
CSS 는 `width:100%` + `max-width` + `margin-inline:auto` 구조를 쓴다.

**같은 개념에 토큰을 둘 두지 않는다.** 시안용 값 하나만 두고,
CSS 구조에만 필요한 값(상한 등)은 코드에서 처리한다.

---

## 작업 순서

`figma-work-principles` 스킬 「A10. 값을 하드코딩하지 않는다」와
「D. 되돌리기 어려운 작업」을 따른다.
여기에는 Figma 고유 절차만 적는다.

### 컴포넌트를 만들기 전

**파일 전체에서 같은 이름을 검색한다.** `search_design_system` 은 로컬 컴포넌트를 놓치므로
"없다"의 근거로 쓸 수 없다.

기존 컴포넌트를 인스턴스·중첩·슬롯으로 재사용할 수 있는지 먼저 본다.

### 값을 바꾸기 전

**`componentPropertyDefinitions` · `children` 을 그 컴포넌트에 대해 다시 읽는다.**
다른 컴포넌트에서 일반화하지 않는다. 같은 이름의 슬롯도 구조가 다를 수 있다.

완료 검증은 **수치로** 한다 — 좌표·색·개수.
"속성 개수" 같은 대리 지표는 검증이 아니다.

### 삭제하기 전

스킬 「D1. 삭제 전 절차」에 더해, **참조 범위에 다른 프로젝트 파일을 포함한다.**
DS Master 에서 미사용이어도 프로젝트에서 쓰는 토큰이 있다.

**description 이 있는 토큰은 삭제 후보에서 제외한다.** 이유가 적혀 있다는 건 의도적이라는 뜻이다.

---

## 컴포넌트 세트 규격

- 세트 프레임에 **점선 테두리**
- 세트 프레임 배경은 **`surface/default`**
  → 모드를 따라가므로 페이지를 `Theme=Dark` 로 바꾸면 **다크 변형을 그 자리에서 확인**할 수 있다
- **단독 컴포넌트에는 테두리를 넣지 않는다**
- **기존 세트에는 소급 적용하지 않는다**

레이어 순서 규칙은 `figma-work-principles` 스킬 「G2. 레이어 순서」.

### 테두리 규격 — 조회로는 알 수 없다

소급 적용을 안 해서 기존 세트가 섞여 있다. **기존 세트를 조회해도 이 값이 나오지 않는다.**
그래서 여기에 적어둔다.

| 항목 | 값 |
| --- | --- |
| 색 | `#9747ff` <!-- 예외: Figma 에 기준 원본이 없다 --> |
| 두께 · 위치 | 1px INSIDE <!-- 예외: Figma 에 기준 원본이 없다 --> |
| 대시 | 10 / 5 <!-- 예외: Figma 에 기준 원본이 없다 --> |
| radius | 5 <!-- 예외: Figma 에 기준 원본이 없다 --> |
| 패딩 | 40 <!-- 예외: Figma 에 기준 원본이 없다 --> |

나중에 기준 세트나 변수로 옮기면 여기서 뺀다.

---

## 토큰 위생

검사 스크립트가 잡는 항목들의 근거.

### 네이밍

- **같은 뜻에 같은 접미사를 쓴다.** 보조 텍스트는 `fg-supporting` 하나로 통일한다
- **접미사 순서를 뒤집지 않는다.** `supporting-fg` 금지
- **같은 성격의 그룹은 형식을 맞춘다.** `text/primary` 와 `icon/primary` 처럼
- **깊이는 3단계 이하.** 4단계가 필요하면 하이픈으로 합친다 (`calendar/cell/selected-fg`)

### 참조 구조

- **별칭이 자기 자신으로 돌아오면 안 된다** (순환)
- **별칭 사슬은 3단계 이하.** 더 깊으면 계층 설계가 잘못됐다는 신호다
- **존재하지 않는 변수를 가리키는 참조가 없어야 한다**

### 메타데이터

**세 가지 모두 빠짐없이 채운다** — 검사 항목 D12 가 잡는다.

| 항목 | 무엇 | 규칙이 있는 곳 |
| --- | --- | --- |
| `codeSyntax` | 코드 생성에 쓰는 이름 | 스킬 「E4. 바인딩」 |
| `scopes` | Figma UI 목록에 뜨는 자리를 좁힌다 | 스킬 「E3. scopes」 |
| `description` | 왜 필요한지. 없으면 삭제 후보로 올라간다 | 스킬 「D2. description 이 있으면 삭제 후보에서 제외한다」 |

### 수치는 문서에 적지 않는다

**이 규칙은 이 문서만이 아니라 스킬 · 인수인계 메모 · 회의록에도 적용된다.**
값이 아니라 **토큰 이름**을 쓴다.

    헤더 높이 90px          // 금지 — 값이 바뀌면 낡는다
    헤더 높이는 header/height  // 정상

값이 사는 곳은 세 곳뿐이다.

| 층                    | 담는 것              |
| --------------------- | -------------------- |
| Figma 변수            | 값 (정본)            |
| `figma/tokens.*.json` | 값 스냅샷            |
| `tokens/*.scss`       | 값 (스냅샷에서 옮김) |

**나머지 전부는 규칙과 이유만 담는다.**
예외는 Figma 에 원본이 없어 조회로 알 수 없는 것뿐이다 (아이콘 규격 · 세트 컨테이너 스타일).

### 모드 값

- **모든 모드에 값이 있어야 한다**
- **브레이크포인트 값은 Wide → Mobile 로 가면서 커지지 않는다.**
  예외가 있으면 `description` 에 이유를 적는다

---

## 증상과 원인을 구분한다

이 프로젝트에서 실제로 겪은 것 — **단정하지 말고 재현되면 실측한다.**

- `maxWidth` 를 "바인딩 불가"로 오진했다. 실제 원인은 라이브러리 변수 조회 실패였다
- 인스턴스 `HUG` 를 "불가"로 오진했다. `resize` 실패와 뭉뚱그린 것이었다

**증상과 원인은 다르다.** 동작하지 않으면 왜 그런지 먼저 좁힌다.

---

## 아이콘

### 규격 — 조회로는 알 수 없다

Figma 에 기준 원본이 없어 조회로 정답을 알 수 없다. 나중에 기준 세트나 변수로 옮기면 여기서 뺀다.

듀오톤 아이콘: 40×40 프레임 · 글리프 긴 변 30px · strokeWeight 1.6 · CENTER · ROUND · ROUND <!-- 예외: Figma 에 기준 원본이 없다 -->

색은 본체와 강조 두 가지만 쓴다.

**Scale 도구(K)를 쓰지 않는다.** strokeWeight 가 함께 곱해져 아이콘마다 굵기가 갈린다.
실제로 이 이유로 굵기가 여러 종으로 벌어진 적이 있다.
크기를 맞출 때는 프레임 안에서 글리프를 리사이즈한 뒤 strokeWeight 를 명시적으로 재설정한다.

### 네이밍

Figma 교체 드롭다운은 이름의 `/` 경로로 그룹핑된다.
그래서 **스타일 세그먼트를 경로에 넣어** 세트를 분리한다.

| 세트 | 규칙 |
| --- | --- |
| 듀오톤 (주 사용) | `icon/duo/*` |
| 라인 (Feather 계열) | `Icon/*` — 대문자. 무변경 |
| 향후 라인 버전 | `icon/line/*` |

이름은 아이콘이 나타내는 사물·동작을 소문자 kebab-case 로 쓴다.

**리네임은 안전하다** — ID 참조라 인스턴스·교체 기본값·추천값이 모두 보존된다.
다만 **퍼블리시된 상태면 이름 변경 반영을 위해 재퍼블리시가 필요하다.**

중복 이름은 허용한다 — Figma 가 ID 로 구분한다. 다만 드롭다운에 같은 이름이 둘 보인다.

---

## 퍼블리시 순서

```
프로젝트 파일의 참조 정리 (삭제·이름변경한 토큰)
  → DS Master 퍼블리시
  → figma/tokens.*.json 재추출
  → 프로젝트에서 라이브러리 업데이트 수락
  → 인스턴스 재조회로 실측
  → 코드 반영
```

**반대로 하면 시안과 코드가 어긋난다.**

---

## 검증

값 대조는 사람이 하지 않는다. **스크립트가 한다.**

검사 항목은 `scripts/README.md` 에 있다.
**새 문제가 나오면 항목을 추가한다.** 항목은 줄지 않는다.

---

## 확인이 필요할 때

`DECISIONS.md` 에 없는 판단이 필요하면 **작업을 멈추고 보고한다.**
스스로 정하지 않는다. 결정이 나면 `DECISIONS.md` 에 기록한다.

**다음은 멈출 이유가 아니다** — 답이 이미 있다.

| 상황 | 답 |
| --- | --- |
| 값이 얼마인가 | Figma 를 조회한다 |
| 프리미티브를 직접 써도 되나 | 「토큰 계층」의 "프리미티브 직접 참조가 정상인 경우" |
| 다크값이 왜 같은가 | 「컬렉션과 모드」 · ADR-002 |
| 어디까지 했나 | `STATUS.md` |
| 검사 항목이 무엇인가 | `scripts/README.md` |

---

## 실패했던 사례

같은 실수를 막기 위한 기록. **틀릴 때마다 한 줄 추가한다.**

| 사례 | 원인 |
| --- | --- |
| 툴팁이 다크에서 흰색 | 폐기된 문서의 옛 값을 코드에 넣음 |
| 체크박스가 다크에서 흰 덩어리 | 프리미티브 직접 참조 |
| 이미 있는 컴포넌트를 중복 생성 시도 | 한 번 조회로 "없다" 단정 |
| 셀 데이터 소실 | 프로브 없이 일괄 변경 |
| 로고 중복 생성 후 삭제 | "내가 만든 것"이라 확인 생략 |
| 인스턴스 내부를 자체 레이어로 세어 과대 보고 | 소유 구분 안 함 |
| 상위 N개만 출력하고 "전부"라 단정 | 범위를 밝히지 않음 |
| 이름만 바꾸고 "완료" 보고 | 참조 구조를 안 고침 |
| 컨테이너 패딩 토큰을 화면 여백에 씀 | 토큰 용도 확인 안 함 |
| 세트 배경 통일에서 일부 누락 | 전수 확인 안 함 |
| API 제약을 두 번 오진 | 증상과 원인을 구분 안 함 |
| 수치만 보고 두 변형을 "동일"이라 판정 | 스크린샷·`visible` 플래그·내부 요소 미확인 |
| 변수 이름만 보고 값을 추론 | 이름과 값이 어긋난 변수가 실제로 있었다 |
| 토큰이 "미사용"이라 판정했다가 뒤집힘 | 노드를 하나씩 조회. 페이지 전수 스캔을 안 함 |
| 교체 속성을 변형마다 다르게 두려 했다 | 교체 속성은 세트 전체에 값이 하나다 |
| 문서·스킬·메모에 값을 적어 낡음 | 방침과 값을 섞음 |
| 문서 참조를 장 번호로 걸어 전부 끊김 | 번호는 문서가 자라면 밀린다. 제목으로 건다 |
