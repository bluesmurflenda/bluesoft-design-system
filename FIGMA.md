# Figma 작업 규칙 — BLUESOFT DS Master

**Figma 를 고치는 사람이 읽는 문서다.** SCSS 는 `SCSS.md` 에, 지금 할 일은 `STATUS.md` 에 있다.

여기에는 **조회해도 알 수 없고, 자동 검사도 못 잡는 것만** 있다.
Figma API 동작·조회·검증·삭제 절차·대비 기준은 `figma-work-principles` 스킬에 있다.

`DECISIONS.md` 에 없는 판단이 필요하면 **멈추고 보고한다.** 스스로 정하지 않는다.

---

## 토큰 계층

```
Primitive          색 램프·space·radius·font·motion. hex 가 있는 유일한 곳
  ↓ 별칭
Theme (시맨틱)     surface · text · border · icon · brand · accent
  ↓ 별칭
Theme (컴포넌트)   button · card · field · nav · table · tabs …
  ↓ 참조
컴포넌트 / CSS
```

**컴포넌트는 시맨틱을 거친다.** 프리미티브를 직접 참조하면 다크모드에서 그대로 남는다.
검사 D1 이 잡지만, 아래는 예외라 판정 전에 봐야 한다.

| 프리미티브 직접 참조가 정상인 경우 | 이유 |
| --- | --- |
| 시맨틱 계층 자체 | 프리미티브를 가리키는 것이 이 계층의 역할 |
| 브랜드 램프 (`brand/*` · `accent/*`) | 브랜드 색을 바꾸는 지점 |
| 의미색 (성공·경고·오류·주말) | 의미가 색에 있음 |
| 브랜드 자산 (로고·소셜) | 색이 고정. 모드 무관 |
| 유채색 배경 위 글자 | 대비 확보용 고정 |
| 컴포넌트 고유 톤 | 대응하는 시맨틱이 없음 |
| 알파 토큰 | Figma 별칭으로 알파를 못 넣음 |
| 색이 아닌 것 (`font/*`·`space/*`·`radius/*`) | 모드가 없음 |

**형제를 참조하지 않는다.** 같은 그룹의 다른 하위그룹을 가리키면 두 Variant 가 묶여
하나를 바꿀 때 다른 것도 바뀐다.

```
tabs/pill/fg → tabs/underline/fg   // 금지 — pill 과 underline 이 결합
tabs/pill/fg → text/tertiary       // 정상 — 공통 조상을 각자 참조
```

**값이 같아도 Variant 별로 독립 토큰을 둔다.** 각자 같은 시맨틱을 참조하면 독립과 경유가
동시에 만족된다. 시맨틱을 바꾸면 다 바뀌고, 하나만 조정하려면 그 토큰의 참조 대상만 바꾼다.
형제를 거치면 이게 불가능하다.

---

## `Theme=dark` 는 다크모드가 아니다

`Header` · `_Nav/Menu Item` · `_Side Nav/Item` · `Tooltip` 의 `Theme` 축은 **"어두운 스타일"** 이다.
라이트모드 사이트에서도 어두운 헤더로 쓴다.

```
Theme=light  →  모드를 따라감 (다크모드에서 어두워짐)
Theme=dark   →  항상 어두움
```

**따라서 `*/dark/*` 토큰은 다크모드에서 반전하지 않는다.** 반전하면 "어두운 스타일"이 흰색이 된다.
`tooltip/light/*` 도 마찬가지다. 토큰명에 `/dark/` 나 `/light/` 가 있으면 라이트·다크 값이 같아야 한다.

**`brand/*` · `accent/*` 램프는 다크모드에서 반전된다.** "어두운 스타일" 토큰이 이것을 참조하면
별칭은 같아도 값이 뒤집힌다. 프리미티브로 고정한다.

nav 계열은 **light 만 hover 시 글자색이 바뀐다.** dark 는 전 상태 고정이다.

`Logo` 의 `Color` 축은 성격이 다르다 — 로고 색상 선택이다.

---

## 컬렉션과 모드

| 컬렉션 | CSS |
| --- | --- |
| Primitive | `:root` |
| Theme | `:root` + `[data-theme="dark"]` |
| Shape | `:root` + `[data-shape="…"]` |
| Breakpoint | `:root` + 미디어쿼리 |

**모드는 실제로 값이 갈릴 때만 만든다.** 모드가 하나 늘면 앞으로 추가하는 모든 토큰에
값을 하나씩 더 채워야 한다.

**모드와 프레임 폭은 다르다.** 큰 폭으로 시안을 그린다고 그 모드가 필요한 것은 아니다.
프레임 폭만 크게 두고 모드는 낮은 것을 쓴다 — ADR-026.

**최상단(Wide) 은 미디어쿼리로 쓰지 않는다.** 브라우저마다 걸리는 폭이 다르고,
Wide 값은 Desktop 과 같아서 분기할 이유가 없다. 근거는 ADR-026.

**부모 폭을 따라가는 노드에는 폭 변수를 물리지 않는다.** `FILL` 로 바꿔도 폭 바인딩은
자동으로 풀리지 않는다 — 따로 푼다. 방법은 스킬 「E4. 바인딩」.

---

## 슬롯과 크기

**슬롯은 이름이 아니라 사이징으로 고른다.** `Table/Cell` 의 슬롯 이름은 실제 용도와 어긋나 있다.
인스턴스 오버라이드 유실 위험 때문에 이름을 유지하기로 확정했다.

| 사이징 | 넣을 수 있는 것 |
| --- | --- |
| FIXED | 크기가 고정된 것 (아이콘·아바타) |
| HUG | 폭이 가변인 것 (Chip · Avatar Group) |

FIXED 슬롯에 가변폭 컴포넌트를 넣으면 눌린다.
컬럼 폭이 다른 테이블은 `Table/Row` 를 쓰지 않고 **셀 인스턴스를 오토레이아웃으로 나열한다.**
디태치가 아니다 — 셀은 인스턴스로 남아 마스터 변경을 계속 받는다.

**높이는 고정하지 않고 `min-height` 로 둔다.** 여백은 `padding-y` 가 아니라 세로 중앙 정렬이
만든다. 둘을 함께 쓰면 여러 줄에서 높이가 과도해진다.

**같은 개념에 토큰을 둘 두지 않는다.** Figma 는 `width: 100%` 를 표현할 수 없어 폭 값이
필요하지만, CSS 구조에만 필요한 값(상한 등)은 토큰으로 만들지 않고 코드에서 처리한다.

---

## 작업 순서

스킬 「A10. 값을 하드코딩하지 않는다」와 「D. 되돌리기 어려운 작업」을 따른다.
여기에는 이 프로젝트 고유 절차만 적는다.

**만들기 전** — 파일 전체에서 같은 이름을 검색한다. `search_design_system` 은 로컬 컴포넌트를
놓치므로 "없다"의 근거로 쓸 수 없다. 기존 컴포넌트를 인스턴스·중첩·슬롯으로 재사용할 수
있는지 먼저 본다.

**값을 바꾸기 전** — `componentPropertyDefinitions` · `children` 을 그 컴포넌트에 대해 다시 읽는다.
다른 컴포넌트에서 일반화하지 않는다. 같은 이름의 슬롯도 구조가 다를 수 있다.

**삭제하기 전** — 스킬 「D1. 삭제 전 절차」에 더해 **참조 범위에 다른 프로젝트 파일을 포함한다.**
DS Master 에서 미사용이어도 프로젝트에서 쓰는 토큰이 있다.
**description 이 있는 토큰은 삭제 후보에서 제외한다.**

---

## 토큰 위생 — 사람이 MCP 로 검사한다

`D3~D9 · D11~D13` 은 스크립트가 아니라 **사람이 Figma MCP 로 돌린다**(ADR-017).
그래서 이 절이 그 검사의 판정 기준이다. 자동으로 도는 검사는 이 목록에 없다.

**언제 돌리나** — Figma 를 수정한 뒤, 퍼블리시하기 전에. 코드 작업 중에는 돌리지 않는다.

**네이밍**

- 같은 뜻에 같은 접미사를 쓴다. 보조 텍스트는 `fg-supporting` 하나로 통일
- 접미사 순서를 뒤집지 않는다. `supporting-fg` 금지
- 같은 성격의 그룹은 형식을 맞춘다. `text/primary` 와 `icon/primary` 처럼
- 깊이는 3단계 이하. 더 필요하면 하이픈으로 합친다

**참조 구조**

- 별칭이 자기 자신으로 돌아오면 안 된다
- 별칭 사슬이 3단계보다 깊으면 계층 설계가 잘못됐다는 신호다
- 존재하지 않는 변수를 가리키는 참조가 없어야 한다

**메타데이터** — 세 가지를 빠짐없이 채운다.

| 항목 | 무엇 | 규칙 |
| --- | --- | --- |
| `codeSyntax` | 코드 생성에 쓰는 이름 | 스킬 「E4. 바인딩」 |
| `scopes` | Figma UI 목록에 뜨는 자리를 좁힌다 | 스킬 「E3. scopes」 |
| `description` | 왜 필요한지 | 스킬 「D2. description 이 있으면 삭제 후보에서 제외한다」 |

**모드 값** — 모든 모드에 값이 있어야 하고, 브레이크포인트 값은 큰 폭에서 작은 폭으로 가면서
커지지 않는다. 예외가 있으면 `description` 에 이유를 적는다.

---

## 조회로는 알 수 없는 수치

Figma 에 기준 원본이 없어 조회해도 정답이 안 나온다. **문서에 값을 적는 유일한 예외다.**
나중에 기준 세트나 변수로 옮기면 여기서 뺀다.

**컴포넌트 세트 컨테이너** — 점선 테두리 `#9747ff`, 1px INSIDE, 대시 10/5, radius 5, 패딩 40. <!-- 예외: Figma 에 기준 원본이 없다 -->
배경은 `surface/default` 로 둔다 — 모드를 따라가므로 페이지를 `Theme=Dark` 로 바꾸면
다크 변형을 그 자리에서 확인할 수 있다.
단독 컴포넌트에는 테두리를 넣지 않는다. 기존 세트에는 소급 적용하지 않아서 섞여 있다.

**듀오톤 아이콘** — 40×40 프레임, 글리프 긴 변 30px, strokeWeight 1.6, CENTER·ROUND·ROUND. <!-- 예외: Figma 에 기준 원본이 없다 -->
색은 본체와 강조 두 가지만 쓴다.

**Scale 도구(K)를 쓰지 않는다.** strokeWeight 가 함께 곱해져 아이콘마다 굵기가 갈린다.
크기를 맞출 때는 프레임 안에서 글리프를 리사이즈한 뒤 strokeWeight 를 다시 설정한다.

---

## 아이콘 네이밍

Figma 교체 드롭다운은 이름의 `/` 경로로 그룹핑된다. 그래서 **스타일 세그먼트를 경로에 넣어**
세트를 분리한다.

| 세트 | 규칙 |
| --- | --- |
| 듀오톤 (주 사용) | `icon/duo/*` |
| 라인 (Feather 계열) | `Icon/*` — 대문자. 무변경 |
| 향후 라인 버전 | `icon/line/*` |

이름은 아이콘이 나타내는 사물·동작을 소문자 kebab-case 로 쓴다.

**리네임은 안전하다** — ID 참조라 인스턴스·교체 기본값·추천값이 보존된다.
다만 퍼블리시된 상태면 이름 변경 반영을 위해 재퍼블리시가 필요하다.
중복 이름은 허용한다. Figma 가 ID 로 구분한다.

---

## 퍼블리시 순서

```
프로젝트 파일의 참조 정리 (삭제·이름변경한 토큰)
  → DS Master 퍼블리시
  → figma/tokens.*.json 재추출
  → 프로젝트에서 라이브러리 업데이트 수락
  → 인스턴스 재조회로 실측
  → 코드 반영
```

**반대로 하면 시안과 코드가 어긋난다.**
Icon Set 을 고친 날은 DS Master 도 함께 퍼블리시한다. 둘 다 수동이다.

---

## 검사가 못 잡는 실수

자동 검사가 잡는 것은 여기 적지 않는다. **판단이 틀린 사례만 남긴다.**

| 사례 | 원인 |
| --- | --- |
| 이미 있는 컴포넌트를 중복 생성 시도 | 한 번 조회로 "없다" 단정 |
| 토큰이 "미사용"이라 판정했다가 뒤집힘 | 페이지 전수 스캔을 안 함 |
| 두 변형을 "동일"이라 판정 | 스크린샷·`visible`·내부 요소 미확인 |
| 변수 이름만 보고 값을 추론 | 이름과 값이 어긋난 변수가 실제로 있었다 |
| 교체 속성을 변형마다 다르게 두려 했다 | 교체 속성은 세트 전체에 값이 하나다 |
| API 제약을 두 번 오진 | 증상과 원인을 구분 안 함. 재현되면 실측한다 |
| 이름만 바꾸고 "완료" 보고 | 참조 구조를 안 고침 |
| 셀 데이터 소실 | 프로브 없이 일괄 변경 |
