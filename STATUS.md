# 진행 상태

**작업을 끝낼 때마다 여기를 갱신한다.** 세션이 끊겨도 이것만 읽으면 재개할 수 있다.

끝난 일의 상세 기록은 두지 않는다 — git 로그에 있다.
결정은 `DECISIONS.md`, 규칙은 `FIGMA.md` · `SCSS.md`.

**여기에 값·수치·개수를 적지 않는다.** 검사를 돌려서 확인한다.

---

## 지금 하는 일

```
8단계 — Figma ↔ SCSS 색 바인딩 전수 대조
방법은 SCSS.md 「각 컴포넌트 작업 흐름」, 검사 항목은 scripts/README.md D14
```

| 단계 | 대상 | 상태 |
| --- | --- | --- |
| 0~7 | 스냅샷 추출 · 검사 스크립트 · `tokens/` · `abstracts/` · 버튼 · 입력 · 상태 표시 · 테이블 · 내비게이션 | 완료 |
| 8 | 색 바인딩 전수 대조 | 진행 |

8단계 안에서 끝낸 컴포넌트만 적는다. 시작할 때 행을 추가한다.

| 컴포넌트 | 상태 |
| --- | --- |
| Alert | 검사통과 |

`검사통과` = `npm run check:nodes` · `check:tokens` · `build` 셋 다 통과.

---

## 막힌 것 — 사람이 결정해야 진행되는 것

결정이 나면 `DECISIONS.md` 로 옮기고 여기서 지운다.

| 무엇 | 왜 막혔나 |
| --- | --- |
| Wide 구간 미디어쿼리를 남길지 지울지 | 그 구간의 여백값은 컨테이너 폭에서 계산되는 값이라 CSS 가 자동으로 만든다. 토큰을 유지할 근거가 필요하다 |
| 태블릿 구간에서 헤더 메뉴가 어떻게 보여야 하나 | Figma 에 그 구간 변형 자체가 없어 근거가 없다. 현재 코드는 모바일 기준폭 이상에서 이미 풀 메뉴를 보여준다 |
| `grid/margin` 과 `container/padding-x` 가 데스크톱 아래에서 같은 값이다 | 하나로 합칠 수 있는지는 Figma 쪽 판단이다 |

---

## 지금 어긋난 것

**문서·코드와 Figma 라이브가 다르면 여기에 적는다. 라이브가 이긴다.**
처리하면 행을 지운다. 처리 이력은 남기지 않는다.

| 대상 | 내용 |
| --- | --- |
| `figma/tokens.primitive.json` · `tokens.breakpoint.json` | 스냅샷이 낡았다. 퍼블리시 후 재추출을 빠뜨렸다 — 재추출 필요 |
| `scss/tokens/_breakpoint.scss` | 섹션 간격·패딩 CSS 변수가 Figma 에서 리네임되기 전 이름을 쓰고 있다. 서브비주얼 높이는 별칭이 아니라 리터럴로 박혀 있다 |
| `scss/components/_input.scss` · `_textarea.scss` · `_select.scss` | 입력 계열 radius 가 버튼 토큰을 참조한다. 입력 전용 토큰이 생겼으므로(ADR-027) 교체 대상. 옆 주석은 이제 틀린 내용이다 |
| 컴포넌트별 인라인 hex | 옛 프리미티브 램프 값이 남아 있다. `check:tokens` S2 로 잔여 확인 필요 |
| `tabs/underline` 계열 hover 토큰 | Theme 컬렉션에 있지만 해당 컴포넌트 세트 전수 조회에서 어디에도 바인딩되지 않는다. 다른 컴포넌트가 쓰는지 또는 미사용인지 확인 필요 |
| `_List/Item` 의 선택 배경·보조 텍스트 토큰 | Figma 에 있으나 코드가 hover 를 재사용하고 보조 텍스트는 없다 |

---

## 사람이 해야 하는 것

| 무엇 |
| --- |
| Field/Group 의 `Addon` 변형 축 옵션 순서 정리 (Figma UI 에서만 가능) |
| DS Master 재퍼블리시 — 그 뒤에 `figma/tokens.*.json` 재추출 |
| `hospital-content-design` 스킬의 낡은 토큰 이름 정리 |
| `DECISIONS.md` 미결 항목 중 이미 선례가 생긴 것들의 확정 |
