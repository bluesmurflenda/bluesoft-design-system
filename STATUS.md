# 진행 상태

**작업을 끝낼 때마다 갱신한다.** 세션이 끊겨도 이것만 읽으면 재개할 수 있다.
끝난 일은 지운다 — git 로그에 있다. 값·수치·개수를 적지 않는다.

**검사가 판정할 수 있는 것은 여기 적지 않는다.** `npm run check` 를 돌리면 매번 새로 나온다.
문서가 검사 결과의 사본을 들고 있으면 반드시 어긋나고, 어긋난 사본은 없는 것보다 나쁘다.

---

## 지금 하는 일

**8단계 — Figma ↔ SCSS 바인딩 전수 대조.** 0~7단계 완료(ADR-033 리네임 커밋됨).
**색만이 아니라 바인딩된 값 전부를 본다**(ADR-034). 색만 보다가 Alert 반경을 놓쳤다.
방법은 `SCSS.md` 「컴포넌트 하나를 끝내는 흐름」+「클래스·변형 설계」, 검사 항목은 `scripts/README.md`.

**시작 전에 `.env` 의 `FIGMA_TOKEN` 을 확인한다.** 없으면 Figma 노드 검사가 건너뛰는데
(그건 의도된 동작이다 — `SCSS.md` 「완료 판정」), 8단계는 색 대조가 절반이라 건너뛰면 반쪽만 본다.

**순서는 안에 들어가는 것부터다.** 다른 컴포넌트가 참조하는 뿌리를 먼저 확정한다 —
input → btn → badge → table, 그다음 거기 붙는 것들. 제외 대상은 logo · login-header ·
con · header · side-nav-item.

8단계 뒤에는 **아직 SCSS 가 없는 컴포넌트를 만든다** — 배포(npm 패키지, ADR-032)는 그 다음이다.
목록은 여기 적지 않는다. Figma 컴포넌트 세트 전수와 대조해 그때 확인한다.

**끝낸 컴포넌트는 아직 없다.** Alert 은 색과 반경만 맞췄고 Figma 쪽 여백이 남아 있다.

---

## 지금 어긋난 것

**위에서부터 처리한다.** 처리하면 행을 지운다. 이력은 남기지 않는다.

### Figma 쪽

> **바른 기준으로 전 페이지를 다시 쟀다** — 컴포넌트 세트 프레임 제외, 인스턴스 안쪽 제외,
> **효과가 있는 속성만**(값이 0 인 여백, 반경 0, 테두리 없는 노드의 굵기는 세지 않는다).
> 이전 목록은 이 기준이 아니어서 틀린 항목이 있었고 지웠다.
> **개수는 여기 적지 않는다** — 검사가 붙으면 검사가 낸다. 여기는 어느 컴포넌트인지만 적는다.
>
> **아래는 치수 기준이다. 색은 `check:nodes` 가 본다.**
>
> **이 표가 왜 손으로 적혀 있나 — 치수를 보는 검사가 아직 없다.** `check:nodes` 는 색만 스캔하고
> `check:tokens` 는 코드와 스냅샷만 본다. **ADR-034 는 사람이 대조하는 범위를 넓혔을 뿐
> 스크립트는 그대로다.** 그래서 여기 적어 둔다 — **치수 검사가 붙으면 이 표는 지운다.**

| 대상 | 무엇을 해야 하나 |
| --- | --- |
| **치수 기준으로 깨끗한 것 — 다시 보지 않는다** | Alert · Accordion 둘 · Calendar 둘 · Badge · Badge/dot · ButtonGroup · Checkbox · Input · Textarea · Field/Group · Progress bar · Scrollbar · Pagination 의 번호 · Table 계열 전부 · board 계열 전부 · Avatar 색 견본 |
| **테두리 두께만 비어 있는 것** | Button · Icon Button · Avatar · Avatar Group · Checkbox·Radio·Input·Textarea 의 base. 값이 이미 있는 프리미티브와 같아 **화면은 안 바뀐다.** 파일 전수로 한 번에 물린다 |
| **여백·간격이 비어 있는 것 — 이제 Figma 에이전트에게 맡길 수 있다** | Social Button · Con · Card · Radio · Toggle · Upload 둘 · Featured Icon · Modal 계열 · Tabs 둘 · Tooltip · Pagination · Select 계열 전부 · Nav 계열. Upload 와 Modal 은 반경까지 비어 있다 |
| Avatar 의 `Online` 반경 | 안 물려 있다. 링 색은 처리했다 |
| **재퍼블리시 + 스냅샷 재추출** | 절반 단계 프리미티브를 새로 만들었다. 재추출해야 S2 가 「코드에 없다」고 잡아 준다 |
| `Table` 의 `Container` 축 옵션 표기 | 한 옵션만 대문자로 시작한다. 같은 성격인 board-list 는 전부 소문자다 |
| Figma `codeSyntax` | 전 변수를 `var(--bds-*)` 로 쓸어야 한다. 리네임 커밋과 같은 시점에 하기로 했는데 코드만 갔다 |
| **헤더 — 지금은 손대지 않는다** | Figma 쪽이 미완성이라 이번 범위에서 뺐다. 다시 볼 때 확인할 것: 모바일 폭 변형에서 안내 바가 데스크톱 폭 그대로 넘친다는 것, 같은 폭 옵션 안에서 일부 변형만 크기 설정이 다르다는 것, 그리고 스케일 밖 값이 이 컴포넌트에 제일 많다는 것 |

### 코드 쪽

| 대상 | 무엇을 해야 하나 |
| --- | --- |
| **변형끼리 치수가 서로 일관된가 — 어느 검사도 안 본다** | 새 기준은 「값이 물려 있나」만 본다. **같은 크기의 변형끼리 값이 서로 같은지는 아무것도 안 본다** — 한 변형만 다른 값을 물고 있어도 전부 통과한다. 치수 검사 `[2]` 로 넘겼고, 붙기 전까지는 **사람이 본다** |
| `scripts/README.md` 의 스냅샷 추출 방법 | 적혀 있는 스니펫이 실제 파일과 다른 모양을 만든다. 실제로 쓴 방법으로 고쳐 둔다 |

---

## 사람이 해야 하는 것

- **절반 단계를 스케일에 추가한 것을 ADR 로 남길지 정하기.** 눈금 사이 절반 값 네 개를
  프리미티브로 만들었다. **되돌리려면 그 값을 쓰는 자리를 전부 다시 정해야 하고, 스케일 자체의
  구조를 바꾼 것**이라 ADR 기준에 걸린다. 쓸지 말지 알려주면 그때 쓴다.
  이름에 점을 안 쓴 이유(SCSS 변수 이름에 점을 못 쓴다)도 같이 남긴다
- **ADR-019 가 사실상 비었다는 한 줄을 남기기.** 그 ADR 이 승인한 프리미티브 직접 참조는
  Avatar 의 Online 링과 Checkbox 의 검증 테두리였는데, **둘 다 Figma 에서 토큰으로 옮겨갔다.**
  검사 쪽 예외 항목은 전부 지웠다(D15 가 "한 번도 안 맞았다"로 잡아 줬다) — 남은 건
  ADR 에 한 줄 남기는 것뿐이다. 새 ADR 이 아니라 한 줄이면 된다
- Field/Group 의 `Addon` 변형 축 옵션 순서 정리 — Figma UI 에서만 된다.
  확인함: 캔버스 배치 순서와 드롭다운 옵션 순서가 서로 다르다. 바인딩은 전부 정상이다
- **육안 확인 — 커서도 나도 브라우저가 없다. 이번에 화면이 실제로 바뀐 자리들이다.**
  - Avatar 의 Online 링 — 다크에서 흰색이 아니라 표면색을 따라간다
  - 검증 valid 상태의 포커스 링 — 한 단계 진해졌다
  - **Textarea · Select 의 오류 안내문 — 회색에서 빨강으로 바뀐다.** Input 과 맞췄다
  - 스케일 밖 값을 올린 자리들 — Card · Radio · Select · 목록 패널 · 드롭다운 · 페이지네이션의 간격
  - 문서 사이트 라이트·다크 전반
- `DECISIONS.md` 미결 항목 중 이미 선례가 생긴 것들의 확정
