# 진행 상태

**작업을 끝낼 때마다 갱신한다.** 세션이 끊겨도 이것만 읽으면 재개할 수 있다.
끝난 일은 지운다 — git 로그에 있다. 값·수치·개수를 적지 않는다.

**검사가 판정할 수 있는 것은 여기 적지 않는다.** `npm run check` 를 돌리면 매번 새로 나온다.
문서가 검사 결과의 사본을 들고 있으면 반드시 어긋나고, 어긋난 사본은 없는 것보다 나쁘다.

---

## 지금 하는 일

**8단계 — Figma ↔ SCSS 바인딩 전수 대조.** 0~7단계 완료(ADR-033 리네임 커밋됨).
**색만이 아니라 바인딩된 값 전부를 본다**(ADR-034). 색만 보다가 Alert 반경을 놓쳤다.
방법은 `SCSS.md` 「컴포넌트 하나를 끝내는 흐름」+「클래스·변형 설계」, 검사 항목은 `scripts/README.md`.

**시작 전에 `.env` 의 `FIGMA_TOKEN` 을 확인한다.** 없으면 Figma 노드 검사가 건너뛰는데
(그건 의도된 동작이다 — `SCSS.md` 「완료 판정」), 8단계는 색 대조가 절반이라 건너뛰면 반쪽만 본다.

**순서는 안에 들어가는 것부터다.** 다른 컴포넌트가 참조하는 뿌리를 먼저 확정한다 —
input → btn → badge → table, 그다음 거기 붙는 것들. 제외 대상은 logo · login-header ·
con · header · side-nav-item.

8단계 뒤에는 **아직 SCSS 가 없는 컴포넌트를 만든다** — 배포(npm 패키지, ADR-032)는 그 다음이다.
목록은 여기 적지 않는다. Figma 컴포넌트 세트 전수와 대조해 그때 확인한다.

**끝낸 컴포넌트는 아직 없다.** Alert 은 색과 반경만 맞췄고 Figma 쪽 여백이 남아 있다.

---

## 지금 어긋난 것

**위에서부터 처리한다.** 처리하면 행을 지운다. 이력은 남기지 않는다.

### Figma 쪽

> **⚠ 이 목록은 손으로 훑어 만든 것이라 정확도를 보장하지 못한다. 검사가 대신하게 만드는 중이다.**
> 자동으로 도는 검사는 색과 컴포넌트 세트 규격만 본다 — **테두리 두께·여백·간격·반경이 변수에
> 물렸는지는 어느 검사도 보지 않는다.** ADR-034 가 사람이 하는 대조 범위를 색에서 전부로
> 넓혔지만 스크립트 쪽은 그대로다. 그래서 지금은 사람이 적을 수밖에 없고, 실제로 이 방식으로
> 적었다가 틀려서 지운 목록이 있다.
>
> **검사에 치수 항목이 붙으면 이 표는 지운다.** 그때부터 목록도 개수도 검사가 뽑는다.
> **여기 적힌 것은 방향으로만 읽고, 개수로 읽지 않는다.**
>
> **색 항목은 여기 적지 않는다** — 검사가 매번 잡아 준다.

| 대상 | 무엇을 해야 하나 |
| --- | --- |
| **`field/hint/fg-invalid` 토큰이 없다** | invalid 상태의 도움말 글자색이다. Figma 도 이 자리에 프리미티브를 직접 물고 있어 코드도 줄 단위 예외로 통과시켰다. 토큰을 만들면 그 예외를 지운다 — 안 지우면 `S9` 가 잡는다 |
| **테두리 두께가 어디에도 안 물려 있다** | 전 페이지에서 예외가 없다. 같은 값의 프리미티브가 이미 있는데 전부 리터럴이다. 개별로 고치지 말고 파일 전수로 훑어 한 번에 물린다 |
| **토큰화가 아예 안 된 컴포넌트들** | Upload 계열 · Social Button · Modal. 반경·여백·간격·테두리 대부분이 리터럴이다. 몇 군데 빠진 게 아니다. 값은 대체로 스케일 안에 있어 작업은 기계적이지만 양이 많다 |
| **치수만 부분적으로 빈 컴포넌트들** | Card · Tabs · Tooltip · Toggle · Radio · Pagination · Alert · Checkbox/Radio 의 base · Avatar. 색은 다 물려 있고 여백·간격·반경만 리터럴이다. Tooltip 은 반경이 화살표까지 전부 비어 있다 |
| **Badge · Badge/dot 의 여백·간격이 거의 다 안 물려 있다** | Table/Cell · board-row · board-card 에서 보이던 것들의 원인이 전부 여기다. 값 일부는 스케일에 있어 그냥 물리면 된다 |
| **Badge 의 큰 크기에서 색에 따라 간격이 다르다** | 여섯 색 중 하나만 다른 값이고, 그 하나만 변수에 물려 있다. 물리다 만 흔적이다. **같은 크기인데 색을 바꾸면 간격이 달라진다** — 눈에 보이는 결함이다 |
| Avatar 의 `Online` 반경 | 안 물려 있다. 링 **색**은 처리했다 — 표면 토큰을 따라가게 바꿨다 |
| **스케일 밖 여백·간격을 눈금으로 옮긴다** | 규칙이 정해졌다 — 눈금 사이 중간값은 위쪽으로 올리고, 홀수는 가까운 쪽으로. 전 페이지에서 해당 자리를 찾아 한 번에 바꾼다 |
| `Table` 의 `Container` 축 옵션 표기 | 한 옵션만 대문자로 시작한다. 같은 성격인 board-list 는 전부 소문자다 |
| Figma `codeSyntax` | 전 변수를 `var(--bds-*)` 로 쓸어야 한다. 리네임 커밋과 같은 시점에 하기로 했는데 코드만 갔다 |
| **헤더 — 지금은 손대지 않는다** | Figma 쪽이 미완성이라 이번 범위에서 뺐다. 다시 볼 때 확인할 것 두 가지: 모바일 폭 변형 전부에서 안내 바가 데스크톱 폭 그대로 넘친다는 것, 그리고 같은 폭 옵션 안에서 일부 변형만 크기 설정이 다르다는 것 |

### 코드 쪽

| 대상 | 무엇을 해야 하나 |
| --- | --- |
| **`base/_reset.scss` 의 `body` 가 다크모드를 안 따라간다** | 글자색·배경이 프리미티브(모드 하나)라 다크에서도 라이트 값 그대로다. 컴파일 결과에 `body` 다크 오버라이드가 없다. **문서 사이트에서는 `docs.css` 가 `body` 를 시맨틱으로 덮어서 가려진다** — 이 저장소를 가져다 쓰는 프로젝트에는 그대로 노출된다. 시맨틱으로 바꾸면 라이트 글자색도 한 단계 밝아지므로 그게 의도인지 판단이 필요하다. S4 대상은 아니다(규칙 3 은 컴포넌트 파일 규칙) |
| `scripts/README.md` 의 스냅샷 추출 방법 | 적혀 있는 스니펫이 실제 파일과 다른 모양을 만든다. 실제로 쓴 방법으로 고쳐 둔다 |

---

## 사람이 해야 하는 것

- **space 스케일 밖 값 처리 — 규칙이 정해졌다.**
  두 부류로 갈렸고 처리 방법이 다르다.
  - 격자 눈금 사이 **정확히 중간**에 있는 값들 — 촘촘한 구간에서 쓸 수 있는 중간값이
    **하나도 빠짐없이 다 나왔다.** 흩어진 예외가 아니라 눈금 사이를 반으로 나눈
    **하위 스케일 한 벌**이고, 서로 무관한 여러 컴포넌트에서 반복해서 쓰인다.
    의도적으로 고른 값으로 보인다
  - **홀수** 값들 — 한 컴포넌트의 한 크기에서만 나온다. 실수로 보인다

  **정해졌다 — 둘 다 눈금으로 옮긴다. 중간값은 위쪽 눈금으로 올린다.**
  홀수는 가까운 쪽으로 간다. 이제 결정이 아니라 Figma 작업이다(아래 「Figma 쪽」)
- **ADR-019 가 사실상 비었다는 한 줄을 남기기.** 그 ADR 이 승인한 프리미티브 직접 참조는
  Avatar 의 Online 링과 Checkbox 의 검증 테두리였는데, **둘 다 Figma 에서 토큰으로 옮겨갔다.**
  검사 쪽 예외 항목은 전부 지웠다(D15 가 "한 번도 안 맞았다"로 잡아 줬다) — 남은 건
  ADR 에 한 줄 남기는 것뿐이다. 새 ADR 이 아니라 한 줄이면 된다
- Field/Group 의 `Addon` 변형 축 옵션 순서 정리 — Figma UI 에서만 된다.
  확인함: 캔버스 배치 순서와 드롭다운 옵션 순서가 서로 다르다. 바인딩은 전부 정상이다
- 문서 사이트 라이트·다크 육안 확인 — 커서는 브라우저 도구가 없어 못 한다
- `DECISIONS.md` 미결 항목 중 이미 선례가 생긴 것들의 확정
