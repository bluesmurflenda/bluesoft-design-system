# 진행 상태

**재개용 메모다.** 세션이 끊겨도 이것만 읽으면 이어서 할 수 있다.

**규칙은 여기 적지 않는다** — 규칙은 `SCSS.md` 와 `FIGMA.md`, 결정은 `DECISIONS.md` 에 있다.
**지금 상태의 세부를 적지 않는다** — 어느 컴포넌트가 어떤지, 개수, 값. 전부 반드시 낡는다.
**검사가 판정할 수 있는 것도 적지 않는다** — `npm run check` 가 매번 새로 낸다.

**고칠 때는 덧붙이지 않는다. 이전 줄을 지우고 바꾼다.**

---

## 지금 하는 일

**8단계 — 컴포넌트를 완성한다.** 0~7단계 완료. 방법은 `SCSS.md`.

**대조가 아니라 완성이 목적이다.** Figma 컴포넌트를 손봐야 하면 손보고,
**SCSS 가 없는 컴포넌트를 만나면 그 자리에서 만든다.** 뒤로 미루지 않는다.

**제외 대상** — logo · login-header · con · header · side-nav-item.

**바로 쓸 수 있는 상태를 먼저 만든다.** 검사를 완벽하게 만드는 일보다 우선한다.

**배포는 개발자가 담당한다.** 이 저장소는 배포를 준비하지 않는다.

---

## 검사·도구 — 컴포넌트가 끝난 뒤에 한다

**지금은 여기에 시간을 쓰지 않는다.** 순서만 남긴다.

1. CI 를 실제로 돌게 만든다 — 저장소 비밀값이 없어 Figma 노드 검사가 조용히 건너뛴다
2. 스냅샷 재추출을 명령 하나로 만든다
3. 이미 있는 도구를 먼저 붙여 본다
4. 안 덮이는 부분만 검사로 만든다 — 치수, 변형끼리의 일관성
5. Figma `codeSyntax` 를 `var(--bds-*)` 로 쓴다
6. 토큰 관리 방향을 Git 기준으로 바꿀지 정한다

---

## 남은 일

- `npm run build` 가 안 돈다 — sass CLI 가 chokidar 를 require 하다 죽는다(SCSS 내용과 무관).
  지금은 sass JS API 로 컴파일하고 postbuild 두 스크립트를 손으로 돌려 우회한다
- **스냅샷을 전면 재추출해야 한다** — 2026-09-11 에 새로 생긴 변수(Shape 2 · Primitive 4 ·
  Theme 1)만 넣었다. 기존 항목의 값이 그 사이 바뀌었는지는 확인하지 않았다
- 목록 항목·목록 패널의 반경에 맞는 Shape 토큰이 없다 — 각진·Pill 모드를 쓸 때 걸린다
- **Figma 쪽** — Select 트리거와 Scrollbar 는 Shape 토큰이 있는데도 노드가 프리미티브 반경을
  문다. 코드는 Shape 토큰을 쓰고 있어 값은 같지만 모드 전환에서 Figma 만 안 따라온다
- `scripts/README.md` 의 스냅샷 추출 방법이 실제와 다르다
- 코드에서 색과 간격이 바뀐 뒤 **아직 사람이 눈으로 확인하지 않았다** — 커서도 나도 브라우저가 없다
- 눈금 사이 절반 값을 스케일에 넣은 것을 ADR 로 남길지
- ADR-019 를 비었다고 정리할지
- Nav 계열의 라이트·다크를 변형 축에 둘지 모드로 옮길지
- `DECISIONS.md` 미결 항목 중 선례가 생긴 것들의 확정
