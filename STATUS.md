# 진행 상태

**작업을 끝낼 때마다 갱신한다.** 세션이 끊겨도 이것만 읽으면 재개할 수 있다.
끝난 일은 지운다 — git 로그에 있다. 값·수치·개수를 적지 않는다.

---

## 지금 하는 일

**8단계 — Figma ↔ SCSS 바인딩 전수 대조.** 0~7단계는 완료(0단계 일괄 리네임 = ADR-033).
**색만이 아니라 바인딩된 값 전부를 본다**(ADR-034). 색만 보다가 Alert 반경을 놓쳤다.
방법은 `SCSS.md` 「컴포넌트 하나를 끝내는 흐름」+「클래스·변형 설계」, 검사 항목은 `scripts/README.md` D14.

0단계에서 **새로 추가한 토큰은 선언만 있고 아직 어느 선택자에도 붙지 않았다** —
`button-group/divider` · `con/gray/bg` · `list/item/bg-selected` · `list/item/fg-supporting` ·
`table/border-strong` · `table/header-row/border-top` · `tabs/underline/bg-hover` ·
`tabs/underline/border-hover`. 바인딩은 이 단계에서 붙인다.

**순서는 안에 들어가는 것부터다.** 다른 컴포넌트가 참조하는 뿌리를 먼저 확정한다 —
input → btn → badge → table, 그다음 거기 붙는 것들. 제외 대상은 logo · login-header ·
con · header · side-nav-item.

8단계 뒤에는 **아직 SCSS 가 없는 컴포넌트를 만든다** — 배포(npm 패키지, ADR-032)는 그 다음이다.
목록은 여기 적지 않는다 — Figma 컴포넌트 세트 전수와 대조해 그때 확인한다.

끝낸 컴포넌트만 적는다.

| 컴포넌트 | 상태 |
| --- | --- |
| Alert | 검사통과 |

---

## 지금 어긋난 것

**위에서부터 처리한다.** 처리하면 행을 지운다. 이력은 남기지 않는다.

### Figma 쪽 — ⑥ 커밋이 끝난 뒤에 한다

지금 고치면 스냅샷이 다시 낡는다.

| 대상 | 무엇을 해야 하나 |
| --- | --- |
| **`codeSyntax` 를 `var(--bds-*)` 로 쓸어내기** | ADR-033 으로 코드 쪽 이름에 접두사가 붙었다. Figma 변수 이름은 그대로 두고 `codeSyntax` 만 맞춘다. 검사는 이걸 읽지 않고 기계적 변환을 쓰므로 코드 작업을 막지는 않는다 |
| **테두리 두께가 어디에도 안 물려 있다** | 전 페이지 감사가 끝났고 예외가 없다. 같은 값의 프리미티브가 이미 있는데 전부 리터럴이다. 개별로 고치지 말고 파일 전수로 훑어 한 번에 물린다 |
| **토큰화가 아예 안 된 컴포넌트들이 있다** | Upload 계열 · Social Button · Modal 이다. 반경·여백·간격·테두리 대부분이 리터럴이다. 다른 컴포넌트처럼 몇 군데 빠진 게 아니다. 값은 대체로 스케일 안에 있어 물리는 작업 자체는 기계적이지만 양이 많다 |
| **치수만 부분적으로 빈 컴포넌트들** | Card · Tabs · Tooltip · Toggle · Radio · Pagination · Alert · Checkbox/Radio 의 base · Avatar 다. 위와 달리 색은 다 물려 있고 여백·간격·반경만 리터럴이다. Tooltip 은 반경이 화살표까지 전부 비어 있고, **Alert 은 「끝낸 컴포넌트」에 올라 있는데도 여백이 안 물려 있다** — 그때 대조 범위가 색뿐이었다(ADR-034 가 넓힌 그 범위다) |
| **Badge · Badge/dot 의 여백·간격이 거의 다 안 물려 있다** | Table/Cell · board-row · board-card 에서 보이던 것들의 원인이 전부 여기다. 값 일부는 스케일에 있어 그냥 물리면 된다 |
| **Badge 의 큰 크기에서 색에 따라 간격이 다르다** | 여섯 색 중 하나만 다른 값이고, 그 하나만 변수에 물려 있다. 물리다 만 흔적이다. **같은 크기인데 색을 바꾸면 간격이 달라진다** — 눈에 보이는 결함이다 |
| `Table` 의 `Container` 축 옵션 표기 | 한 옵션만 대문자로 시작한다. 같은 성격인 board-list 는 전부 소문자다 |
| **Avatar 의 Online 표시** | 링 색에 대응하는 Figma 토큰이 없어 프리미티브를 직접 물고 있고(ADR-019 로 예외 처리됨), **같은 노드의 반경도 안 물려 있다.** 토큰을 만들지는 아래 「사람이 해야 하는 것」에서 정한다. 만들기로 하면 반경과 한 번에 처리한다 |

**Accordion · ButtonGroup 도 여기 들어간다** — 색을 프리미티브에 직접 물거나 리터럴로
박아 두었다. `check:nodes` 가 잡는다.

> **위 목록은 「무엇에 물려 있나」를 안 본 감사에서 나왔다. 다시 세지 말고 `check:nodes` 로 뽑는다.**
> 물려 있기만 하면 통과시키는 방식으로 훑었더니, 프리미티브를 직접 문 것과 컴포넌트 세트
> 프레임에 박힌 것을 통째로 놓쳤다. 저장소에 이미 그 규칙을 담은 검사가 있는데 따로 훑은 것이
> 잘못이다. 여백·간격이 스케일 밖인지 모으는 것만 따로 하면 된다.

### 코드 쪽

| 대상 | 무엇을 해야 하나 |
| --- | --- |
| `scripts/README.md` 의 스냅샷 추출 방법 | 적혀 있는 스니펫이 실제 파일과 다른 모양을 만든다. 이번 재추출은 다른 방법으로 했으므로, 실제로 쓴 방법으로 고쳐 둔다 |
| `scripts/check-tokens.mjs` 의 S3b 키 구분자 | 소스에 NUL 문자가 그대로 박혀 있어 `grep` 이 이 파일을 바이너리로 취급한다. 동작은 정상이라 급하지 않지만 검색이 안 되는 건 불편하다 |

---

## 막힌 것 — 사람이 결정해야 진행되는 것

결정이 나면 `DECISIONS.md` 로 옮기고 여기서 지운다.

| 무엇 | 왜 막혔나 |
| --- | --- |
| Wide 구간의 `grid/margin` 토큰을 남길지 | 그 값은 컨테이너 폭에서 계산되는 값이라 CSS 가 자동으로 만든다. `container/padding-x` 와도 데스크톱 아래에서 같다. 합칠 수 있는지는 Figma 쪽 판단이다 |
| 태블릿 구간에서 헤더 메뉴가 어떻게 보여야 하나 | Figma 에 그 구간 변형이 없어 근거가 없다. 현재 코드는 모바일 기준폭 이상에서 이미 풀 메뉴를 보여준다 |

---

## 사람이 해야 하는 것

- **space 스케일 밖 값을 어떻게 할지 정하기.** 전 페이지 감사가 끝나 목록이 확정됐다.
  **두 부류로 뚜렷하게 갈린다.**
  - 격자 눈금 사이 **정확히 중간**에 있는 값들 — 촘촘한 구간에서 쓸 수 있는 중간값이
    **하나도 빠짐없이 다 나왔다.** 흩어진 예외가 아니라 눈금 사이를 반으로 나눈
    **하위 스케일 한 벌**이고, 서로 무관한 여러 컴포넌트에서 반복해서 쓰인다.
    의도적으로 고른 값으로 보인다
  - **홀수** 값들 — 한 컴포넌트의 한 크기에서만 나온다. 실수로 보인다

  두 부류를 같은 방식으로 처리할 이유가 없다. 앞쪽은 스케일에 하위 단계를 한 벌 더할지,
  뒤쪽은 가까운 눈금으로 옮길지를 따로 정하면 된다.
  **선례가 하나 있다** — Badge 의 한 변형은 이미 스케일 안 값으로 물려 있다
- **Avatar 의 Online 링이 다크모드에서도 흰색이어야 하는지 정하기.** 링은 초록 점과 아바타
  사이를 갈라 주는 테두리다. 지금 라이트·다크 양쪽 다 흰색이고 다크 오버라이드가 없다.
  Figma 도 같다. 그런데 다크모드의 아바타 배경은 어두운 색이라, 배경을 갈라 주는 역할이라면
  배경을 따라가야 할 것 같다. **ADR-019 는 「프리미티브를 직접 물어도 되는가」만 정했고
  「다크모드에서도 흰색이 맞는가」는 정한 적이 없다** — 서로 다른 질문이 한 항목으로 묶여 있었다.
  흰색이 의도라면 그대로 두면 되고, 배경을 따라가야 하면 Figma 에 토큰을 만든다
- Field/Group 의 `Addon` 변형 축 옵션 순서 정리 — Figma UI 에서만 된다.
  확인함: 캔버스 배치 순서와 드롭다운 옵션 순서가 서로 다르다. 바인딩은 전부 정상이다
- DS Master 재퍼블리시, 그 뒤에 `figma/tokens.*.json` 재추출
- `DECISIONS.md` 미결 항목 중 이미 선례가 생긴 것들의 확정
