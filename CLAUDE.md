# BLUESOFT 디자인시스템

전역 `~/.claude/CLAUDE.md` 를 따르되, 이 문서가 우선한다.

**이 문서에는 규칙이 없다.** 누가 무엇을 하고, 어디를 읽는지만 있다.
규칙을 여기에 적으면 이 문서가 커지고, 커지면 모든 세션이 안 쓰는 내용까지 읽는다.

| 파일 키 | 파일 |
| --- | --- |
| `kJD5jv7RNKxLD1hP8oKtBG` | DS Master |
| `T4yvRXEYW0GKecETo3CyYc` | Icon Set |

## 무엇을 읽나

**자기 작업에 해당하는 줄만 읽는다.** 다른 줄은 열지 않는다.

| 하는 일 | 읽는 것 |
| --- | --- |
| Figma 를 고친다 | `FIGMA.md` + `STATUS.md` |
| SCSS 를 쓴다 | `SCSS.md` + `STATUS.md` |

찾을 때만 여는 문서 — 처음부터 읽지 않는다.

| 알고 싶은 것 | 어디 |
| --- | --- |
| 왜 그렇게 정했나 · 미결 사항 | `DECISIONS.md` |
| 검사 항목이 무엇이고 왜 생겼나 | `scripts/README.md` |
| 값 | Figma 를 조회한다. 못 쓰면 `figma/tokens.*.json` |

Figma API 가 실제로 어떻게 동작하는지는 **`figma-work-principles` 스킬**에 있다.
프로젝트와 무관한 공통 지식이라 저장소에 두지 않는다.

**저장소 안에서는 위에 적힌 것이 전부다.** 다른 md 파일을 발견하면 읽지 않는다 — 폐기된 것이다.
**저장소에 있는 것을 다른 데 복사하지 않는다.** 사본은 반드시 낡는다.

## 역할 경계

| 주체                | 하는 일                                                     | 하지 않는 일                    |
| ------------------- | ----------------------------------------------------------- | ------------------------------- |
| **코드 작업(커서)** | SCSS 작성 · Figma **조회** · 검사 실행 · `STATUS.md` 갱신 | **Figma 수정** · 값 추측 · 결정 |
| **Figma 작업**      | 실측 · 수정 · ADR 제안 · 검사 항목 설계                     | 코드 작성                       |
| **사람**            | 시각적 판단 · 확신도 낮음 승인 · 우선순위                   | 값 대조 (스크립트가 한다)       |

**Figma 를 고쳐야 할 상황이 나오면 코드 작업을 멈추고 보고한다.**
조회는 하되 쓰기 API 를 호출하지 않는다.

## 문서를 고치거나 늘릴 때

1. **참조는 제목으로 한다. 장 번호로 하지 않는다** — 번호는 문서가 자라면 밀린다
2. **한 문서를 읽는 동안 다른 문서로 넘어가게 만들지 않는다** — 넘기고 싶어지면 경계를 잘못 자른 것이다.
   예외는 위의 "찾을 때만 여는 문서" 세 개뿐이다
3. **값·수치·개수를 적지 않는다** — 검사 항목 S8 이 막는다
4. **끝난 것은 지운다** — git 이 기억한다
